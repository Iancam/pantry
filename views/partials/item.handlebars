{{!-- item.handlebars --}}
<div class='row item' id='{{_id}}'>
	<h3 class='item-name'>{{name}}</h3>
  <h6 class="item-expiration">{{expiration}}</h6>
  <div class="item-bottom-row">
    <h6 class='item-category'>{{category}}</h6>
    <img src="/img/minus-icon-24.png" class="btn-remove">
  </div>
</div>