<div class='mm-fixed-top unshifted' id='header'>
  <div class="container">
    <div class="left">
      {{#if alt}}
        <a id="menu-toggle" href="#menu"><span class='glyphicon glyphicon-th-list'></span></a>
      {{else}}
{{!--         <div class="dropdown">
          <button class="btn dropdown-toggle" type="button" id="dropdownMenu1" data-toggle="dropdown"> 
            <span class='glyphicon-th-list'><span>
          </button>
          <ul class="dropdown-menu" role="menu" aria-labelledby="dropdownMenu1">
            <li role="presentation"><a role="menuitem" tabindex="-1" href="#">Action</a></li>
            <li role="presentation"><a role="menuitem" tabindex="-1" href="#">Another action</a></li>
            <li role="presentation"><a role="menuitem" tabindex="-1" href="#">Something else here</a></li>
            <li role="presentation" class="divider"></li>
            <li role="presentation"><a role="menuitem" tabindex="-1" href="#">Separated link</a></li>
          </ul>
        </div> --}}

        <div class="btn-group">
          <button type="button" class="btn btn-sm btn-default dropdown-toggle" data-toggle="dropdown">
            Menu
          </button>
          <ul class="dropdown-menu" role="menu">
            <li><a data-toggle="modal" data-target="#pantryModal" href="#">Create a Pantry</a></li>
            <li><a href="/my_pantries">My Pantries</a></li>
            <li><a data-toggle="modal" data-target="#shareModal" href="#">Share</a></li>
            <li class="divider"></li>
            <li><a href="/logout">Logout</a></li>
          </ul>
        </div>
      {{/if}}
    </div>
    <h2>{{pantry_name}}</h2>
    <div class="right">
      {{#if modal}}
        <a href="#" data-toggle="modal" data-target="#{{page}}Modal"><span class='glyphicon glyphicon-plus'></span></a>
      {{/if}}
    </div>
  </div>
</div>
{{> sidebar}}